server {
    listen 80;
    server_name yourdomain.com; # Replace with actual domain

    location / {
        proxy_pass http://localhost:5050; # Matches PORT in server/index.js
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Optional: Serve static files directly if you want Nginx to handle them instead of Node
    # location /assets/ {
    #     alias /path/to/Public/assets/;
    # }
}
